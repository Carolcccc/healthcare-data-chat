# 🔑 Google Gemini API 完整指南

## 1️⃣ 什麼是 API？

**API = Application Programming Interface（應用程序接口）**

簡單說：
- 你的應用 ↔️ Google Gemini AI ↔️ 處理數據並返回結果
- API Key = 你的身份認證（確認是你在使用 Google 的服務）
- 就像登入郵箱一樣，API Key 証明你是誰

## 2️⃣ 獲取 Google Gemini API Key（免費！）

### 方法（只需 5 分鐘）

**第 1 步：訪問 Google AI Studio**

打開瀏覽器，訪問：
```
https://makersuite.google.com/app/apikey
```

**第 2 步：使用 Google 帳戶登錄**

- 如果已登入 Google，會自動進行
- 如果未登入，點擊登錄

**第 3 步：創建 API Key**

1. 頁面會顯示 "API keys"
2. 點擊 **"Create API Key"** 按鈕
3. 選擇 **"Create API key in new project"**

**第 4 步：複製你的 API Key**

```
例如：AIzaSyD-xh...（一長串字符）
```

- ⚠️ **重要：不要分享此 Key！**
- ✅ 只在自己的應用中使用

## 3️⃣ 設置你的應用使用 API Key

### 方法 A：使用 `.env` 文件（推薦）

**第 1 步：創建 `.env` 文件**

在項目根目錄創建文件：
```bash
touch .env
```

**第 2 步：添加 API Key**

編輯 `.env` 文件，添加：
```
GOOGLE_API_KEY=AIzaSyD-xh...（你的實際 Key）
```

**第 3 步：運行應用**

```bash
streamlit run app_gemini.py
```

應用會自動讀取 `.env` 中的 API Key！

---

### 方法 B：在應用界面中輸入（更簡單）

**步驟：**

1. 運行應用：
```bash
streamlit run app_gemini.py
```

2. 應用打開後，在 **左側邊欄** 會看到：
   - "Google API Key" 輸入框
   - 粘貼你的 API Key
   - 點擊 "Initialize Agent"

就這麼簡單！✨

---

## 4️⃣ 部署到 Streamlit Cloud（給別人使用）

### 步驟 1：準備代碼

```bash
# 確保代碼在 GitHub
git add app_gemini.py tools_gemini.py
git commit -m "Add Gemini version"
git push origin main
```

### 步驟 2：在 Streamlit Cloud 部署

1. 訪問：https://share.streamlit.io
2. 用 GitHub 帳號登錄
3. 點擊 **"New app"**
4. 選擇你的倉庫
5. 選擇 **`app_gemini.py`** 作為主文件

### 步驟 3：添加 API Key 到 Secrets

1. 部署頁面會顯示 "Advanced settings"
2. 點擊 **"Secrets"** 標籤
3. 添加（以 TOML 格式）：
```toml
GOOGLE_API_KEY = "AIzaSyD-xh..."（你的實際 Key）
```

4. 點擊 **"Deploy"**

### 完成！🎉

現在任何人都可以通過你的鏈接使用你的應用：
```
https://your-username-healthcare-ai-agent.streamlit.app/
```

---

## 5️⃣ 代碼中 API Key 如何工作

看看 `app_gemini.py` 的工作流程：

```python
# 第 1 步：讀取 API Key（從 .env 或用戶輸入）
api_key = st.text_input("Google API Key", type="password")

# 第 2 步：設置環境變數
os.environ["GOOGLE_API_KEY"] = api_key

# 第 3 步：初始化 Gemini
from langchain_google_genai import ChatGoogleGenerativeAI
llm = ChatGoogleGenerativeAI(model="gemini-pro")

# 第 4 步：Gemini 收到 API Key，驗証是你在使用
# 現在可以開始處理數據了！
```

## 6️⃣ 免費額度說明

### Google Gemini Pro 免費層級：

- ✅ **60 requests/分鐘**（足夠個人使用）
- ✅ **完全免費**（無需信用卡）
- ✅ **適合：** 學習、開發、演示

### 超過限制時：

- 你可以升級到付費方案
- 查看價格：https://ai.google.dev/pricing
- 但通常教育用途不會超限

## 7️⃣ 快速檢查表

### ✅ 本地運行（自己的電腦）

- [ ] 獲得 Google Gemini API Key
- [ ] 創建 `.env` 文件
- [ ] 添加 `GOOGLE_API_KEY=...`
- [ ] 運行 `streamlit run app_gemini.py`
- [ ] 測試應用

### ✅ 部署到雲端（給別人使用）

- [ ] 代碼推送到 GitHub
- [ ] 在 Streamlit Cloud 創建新應用
- [ ] 選擇 `app_gemini.py`
- [ ] 在 Secrets 添加 API Key
- [ ] 點擊 Deploy
- [ ] 分享你的應用鏈接！

## 8️⃣ 常見問題

### Q: API Key 在哪裡？
**A:** https://makersuite.google.com/app/apikey

### Q: API Key 可以免費使用嗎？
**A:** 是的！有免費額度（60 requests/分鐘）

### Q: 如果我忘記 API Key 怎麼辦？
**A:** 到 Google AI Studio 重新生成或刪除舊的

### Q: 可以把 API Key 放在代碼裡嗎？
**A:** ❌ 不要！會被盜用。使用 `.env` 或 Streamlit Secrets

### Q: 部署後需要再設置 API Key 嗎？
**A:** 是的，在 Streamlit Cloud 的 Secrets 中設置一次，然後自動使用

### Q: 可以在生產環境使用嗎？
**A:** 可以，但要監控使用量和成本

## 🚀 立即開始

### 1 分鐘快速開始：

```bash
# 1. 獲取 API Key（訪問上面的 URL）

# 2. 創建 .env 文件
echo "GOOGLE_API_KEY=你的Key" > .env

# 3. 運行應用
streamlit run app_gemini.py

# 完成！在瀏覽器中打開應用 🎉
```

---

## 📚 更多資源

- **官方文檔：** https://ai.google.dev/docs
- **免費層級信息：** https://ai.google.dev/pricing
- **API 參考：** https://ai.google.dev/api/rest

---

**現在你已經了解了一切！可以開始部署你的應用了！** 🌟
